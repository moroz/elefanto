%a{:name => "comment_#{comment.id}"}
.comment.round{:id => "comment_#{comment.id}"}
  %p= comment.text
  - unless comment.website.empty?
    = link_to raw("<b>#{comment.signature}</b>,"), comment.website
  = "#{comment.ip},"
  %span.timestamps= l(comment.created_at, format: :long) + "."
  - unless comment.website.empty?
    Web:
    = link_to comment.website, comment.website
  - if logged_in?
    = link_to "Delete", comment, method: :delete, :confirm => "Are you sure you want to delete this comment?", :class => "btn-xs btn-warning"
