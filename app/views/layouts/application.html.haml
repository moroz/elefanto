<!DOCTYPE html>
%html
  %head
    %title
      = @title ? @title : "Elefanto — Memoirs of an Elephant"
    = stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track' => true
    = stylesheet_link_tag 'w3', 'custom', 'cantoggle', media: 'all', 'data-turbolinks-track' => true
    = javascript_include_tag 'application', 'data-turbolinks-track' => true
    <meta name="viewport" content="width=device-width, initial-scale=1">
    = csrf_meta_tags
  %body
    .w3-container.layout--background
      .w3-row.w3-container.wrapper
        .w3-col.l2
          &nbsp;
        .w3-col.l8.s12
          = render 'shared/header'
          .light-green.w3-container.row2
            - flash.each do |key, value|
              %div{:class => "flash #{key}"}= value
            = yield
          = render 'shared/footer'
        .w3-col.l2
          &nbsp;
