= render post
%a{:name => "comments"}
%section#comments
  - unless post_comments.empty?
    %h3 Comments
    = render post_comments
%a{:name => "new_comment"}
= render 'comments/comment_form', :post => [post,new_comment]
