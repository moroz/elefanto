.row1--header
  .row1__title
    = image_tag "/images/elephant-zh-mobile.png", :id => "elephant"
  %nav.row1__menu.row
    .col-xs-6.text-center
      = link_to t("homepage"), :controller => :pages, :action => :home, :locale => current_locale
    .col-xs-6.text-center
      = link_to t("blog"), :controller => :posts, :action => :index, :locale => current_locale
    .col-xs-6.text-center
      = link_to t("about"), :controller => :pages, :action => :about, :locale => current_locale
    .col-xs-6.text-center
      = link_to t("faq") , :controller => :pages, :action => :faq,  :locale => current_locale
  .w3-center.row1__langs
    = link_to("English", url_for( :locale => :en))
    &bull;
    = link_to("中文", url_for( :locale => :zh))
    &bull;
    = link_to("polski", url_for( :locale => :pl))
